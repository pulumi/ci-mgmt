# WARNING: This file is autogenerated - changes will be overwritten if not made via https://github.com/pulumi/ci-mgmt

archives:
- id: archive
  name_template: "{{ .Binary }}-{{ .Tag }}-{{ .Os }}-{{ .Arch }}"
before:
  hooks:
  - make tfgen
blobs:
- bucket: get.pulumi.com
  folder: releases/plugins/
  ids:
  - archive
  provider: s3
  region: us-west-2
builds:
- binary: pulumi-resource-#{{ .Config.provider }}#
  dir: provider
  env:
  - CGO_ENABLED=0
  - GO111MODULE=on
  goarch:
  - amd64
  - arm64
  goos:
  - darwin
  - windows
  - linux
  hooks:
    post:
    - env GOOS={{ .Os }} GOARCH={{ .Arch }} go clean -cache
    - env GOOS={{ .Os }} GOARCH={{ .Arch }} go clean -modcache
  #{{- if .Config.skipWindowsArmBuild }}#
  ignore:
  - goarch: arm64
    goos: windows
  #{{- else }}#
  ignore: []
  #{{- end }}#
  ldflags:
  #{{- if ge (index .Config "major-version") 2 }}#
  - -X github.com/#{{ .Repository }}#/provider/v#{{ index .Config "major-version" }}#/pkg/version.Version={{.Tag}}
  #{{- else }}#
  - -X github.com/#{{ .Repository }}#/provider/pkg/version.Version={{.Tag}}
  #{{- end }}#
  #{{- if .Config.providerVersion }}#
  - -X #{{ .Config.providerVersion }}#={{.Tag}}
  #{{- end }}#
  #{{- range (index .Config "extra-ld-flags") }}#
  - #{{ . }}#
  #{{- end }}#
  main: ./cmd/pulumi-resource-#{{ .Config.provider }}#/
changelog:
  skip: true
release:
  disable: true
snapshot:
  name_template: "{{ .Tag }}-SNAPSHOT"
